# Back2u app — build-time env (all VITE_* are public/bundled).
VITE_API_URL=https://your-api.onrender.com          # Render API base URL
VITE_APP_URL=https://your-client-web.vercel.app     # this app's own public URL (share links)
VITE_MAPBOX_TOKEN=pk.your_public_mapbox_token       # map page
