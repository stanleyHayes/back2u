node_modules
dist
build
.next
.turbo
coverage
.env
.env.local
.env.*.local
# API production env holds real secrets — never commit it.
# (Frontend .env.production files hold only public VITE_* vars and stay tracked.)
apps/api/.env.production
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea
.expo
ios/Pods
android/.gradle
android/build
*.tsbuildinfo
credentials.txt

# Raven tooling state
.raven/
